version: '3.1'

services:
  release-dashboard:
    image: node:20.18.3
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    command: bash -c "corepack enable && yarn install && yarn start"
    restart: always
    ports:
      - "3999:3000"

